@model IEnumerable<CryptoPriceResult>;

@{
    Layout = "~/Views/Shared/_AppLayout.cshtml";
}

@section scripts
    {
    <link href="~/css/Crypto/Cryptodesign.css" rel="stylesheet" />
    <link href="~/css/bootstrap-grid.css" rel="stylesheet" />
    <script src="~/lib/bootstrap/dist/js/angular.js"></script>
    <script src="~/lib/bootstrap/dist/js/angular-route.js"></script>
    <script src="~/js/simpleControls.js"></script>
    <script src="~/js/cryptoController.js"></script>
    <script src="~/js/app-crypto.js"></script>
}
<br />
<br />
<br />
<br />
<br />
<br />
<div class="container" data-ng-app="app-crypto">
    <div class="row">
        <div class="col-md-6 offset-3">

            <h2>Crypto Prices</h2>

        </div>
    </div>
    @*<div ng-controller="cryptoController as vm" class="col-md-6 offset-3">
            <div class="text-danger" ng-show="vm.errorMessage">{{vm.errorMessage}}</div>
            <wait-cursor display-when="vm.isBusy"></wait-cursor>
            <table class="table table-responsive table-striped">
                <tr ng-repeat="crypto in vm.crypto">
                    <td>{{crypto.name}}</td>
                    <td>{{crypto.price_usd}}</td>
                    <td>{{crypto.price_btc}}</td>
                    <td>{{crypto.total_supply}}</td>
                    <td>{{crypto.percent_change_24h}}</td>

                </tr>
            </table>

            <form novalidate name="newCryptoLike" ng-submit="vm.addLike()">
                <div class="form-group">
                    <label for="name">Name</label>
                    <input class="form-control" type="text" id="name" name="name" ng-model="vm.newLike.name" />
                </div>
                <div class="form-group">
                    <input type="submit" value="add"
                           class="btn btn-success btn-sm"
                           ng-disabled="newCryptoLike.$invalid" />
                    <span ng-show="newCryptoLike.name.$error.required" class="text-warning">Name is required</span>
                </div>
            </form>


        </div>*@
    <div class="container-fluid">
        <table class="table table-responsive table-striped">
            <th>Name</th>
            <th>Price</th>
            <th>volume</th>
            <th> supply</th>
            <th>24hour % change</th>
            <tr></tr>
        </table>

    </div>

</div>